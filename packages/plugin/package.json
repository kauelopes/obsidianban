{
  "name": "@obsidiankan/plugin",
  "version": "0.1.0",
  "private": true,
  "description": "ObsidianKan Obsidian plugin",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "dev": "node esbuild.config.mjs --watch",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@obsidiankan/types": "workspace:*",
    "@types/node": "^22.9.0",
    "builtin-modules": "^5.2.0",
    "esbuild": "^0.28.0",
    "obsidian": "^1.12.3",
    "typescript": "^5.6.3",
    "vitest": "^4.1.9"
  }
}
