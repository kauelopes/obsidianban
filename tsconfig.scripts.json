{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    "rootDir": ".",
    "module": "ESNext",
    "moduleResolution": "Bundler"
  },
  "comment": "Scoped to typechecked scripts. The legacy batch runners predate strict typing and don't pass; add files here as they're cleaned up.",
  "include": ["scripts/sprint-workflow.ts"],
  "exclude": ["node_modules", "dist"]
}
